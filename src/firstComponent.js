export default function FirstComponent({items,ffff,onClick}){
    return(
        <div>
        <h2>First Component</h2>
        {/* {
            items.map((list,index)=>(
                <p key={index}>
                    {list}
                </p>
            ))
        }
        {ffff} */}
        {/* <button onClick={onClick}>ddd</button> */}
        </div>
    )
}